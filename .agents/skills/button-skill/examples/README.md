# Button Examples (Base Theme)

These examples are intentionally minimal and scoped to the base theme.

## Included
- `basic-usage.vue`: primary, secondary-disabled, and submit usage.

## Verification
- Match each example against `projects/button-skill/spec/component-spec.md`.
- Confirm behavior in Storybook base theme before publishing.

