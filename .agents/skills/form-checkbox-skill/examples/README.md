# FormCheckbox Examples

Install:
```bash
yarn add --registry=https://npm.edpl.us @rds-vue-ui/form-checkbox @rds-vue-ui/rds-theme-base
yarn add -D sass-embedded
```

Ensure base theme is imported in app entry:
```js
import '@rds-vue-ui/rds-theme-base/dist/css/rds-theme-base.css';
```
